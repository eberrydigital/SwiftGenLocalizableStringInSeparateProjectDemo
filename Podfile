project 'LocalizableStringsFramework/LocalizableStringsFramework.xcodeproj'

workspace 'Foobar'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'App' do
	project 'App/App.xcodeproj'
end

project 'BarFramework/BarFramework.xcodeproj'

target 'LocalizableStringsFramework' do

project 'LocalizableStringsFramework/LocalizableStringsFramework'
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LocalizableStringsFramework
  pod 'SwiftGen'
end
